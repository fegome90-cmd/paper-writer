"""Skills package for paper-writer.

Contains imported (reusable) and local (project-specific) skills.
Skills are invoked through adapters — never directly by the orchestrator.
See docs/SKILL_ADAPTERS_SPEC.md for the adapter contract.
"""
