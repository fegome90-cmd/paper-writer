strict_tdd: true
context: |
  paper-writer is a Python project.
  Detected markers: pyproject.toml, Makefile.
  Additional evidence: Test-like files detected (110); examples: .venv/lib/python3.14/site-packages/mypy/test/__init__.py, .venv/lib/python3.14/site-packages/mypy/test/config.py, .venv/lib/python3.14/site-packages/mypy/test/data.py, .venv/lib/python3.14/site-packages/mypy/test/helpers.py, .venv/lib/python3.14/site-packages/mypy/test/meta/__init__.py.
  Primary test command: make test.
  Unit tests: pytest (pytest); pytest via Makefile (make test).
  Integration tests: none.
  E2E tests: none.
rules:
  proposal:
    require_problem_statement: true
  spec:
    require_acceptance_criteria: true
  design:
    require_tradeoffs: true
  tasks:
    protect_review_workload: true
  apply:
    test_command: "make test"
  verify:
    test_command: "make test"
testing:
  detected: "2026-05-25"
  runner:
    command: "make test"
    framework: "pytest via Makefile"
  layers:
    unit: "pytest, pytest via Makefile"
    integration: ""
    e2e: ""
  commands:
    unit:
      - scope: "."
        command: "pytest"
        framework: "pytest"
      - scope: "."
        command: "make test"
        framework: "pytest via Makefile"
    integration:
      []
    e2e:
      []
  coverage:
    command: ""
    commands:
      []
quality:
  lint: "make lint"
  lint_commands:
    - scope: "."
      command: "make lint"
      framework: "linter"
  typecheck: ""
  typecheck_commands:
    []
  format: ""
  format_commands:
    []
